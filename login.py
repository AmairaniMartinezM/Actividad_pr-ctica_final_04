# login module

def login(username, password):
    """Placeholder login function.
    Returns True if username and password are non-empty strings.
    """
    if not username or not password:
        return False
    return True
